// Minimal tRPC setup placeholder for Pages Router
// In a real setup, you'll wire up TRPC via Next.js API routes
export default function handler(req, res){
  res.status(200).json({ ok: true, hello: 'Sayang Smash API' })
}
